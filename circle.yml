test:
    pre:
        - make install

    #override:
    #    - make test

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/e25d81b49c9d70020854
